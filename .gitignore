.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.vscode/
.cursor/
*.log

# recipes.json and pump_labels.json are tracked so laptop/desktop stay in sync via git pull/push.
# (They contain COM port names — use a private repo if that matters.)
